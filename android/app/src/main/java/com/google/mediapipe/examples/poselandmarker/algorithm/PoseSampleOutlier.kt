package com.google.mediapipe.examples.poselandmarker.algorithm

class PoseSampleOutlier(sample: PoseSample, detected_class: String, all_classes: String) {
    private val sample = sample
    private val detected_class = detected_class
    private val all_classes = all_classes
}